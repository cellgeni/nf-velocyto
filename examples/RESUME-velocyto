#!/bin/bash

set -euo pipefail

samplefile=actions/samples.txt
config=actions/nextflow.config

[[ -e "$samplefile" ]] || (echo "File "$samplefile" not found" && false)

nextflow run cellgeni/nf-velocyto \
  --SAMPLEFILE $samplefile \
  -c $config \
  -w velocyto-work \
  --ansi-log false \
  -resume
